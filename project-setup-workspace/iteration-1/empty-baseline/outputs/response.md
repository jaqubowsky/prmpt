Here's what I found in the project at `/tmp/eval-projects/empty-project`:

**Files present:**
- `AGENTS.md` — a template with placeholder sections for Project Overview, Architecture Pattern, Project Structure, Data Layer, AI Working Rules (ALWAYS/NEVER), and a PR Checklist. It also references six docs files (`docs/ai/PRODUCT.md`, `ROADMAP.md`, `CONVENTIONS.md`, `PATTERNS.md`, `TESTING.md`, `ARCHITECTURE.md`) that don't exist yet.
- `package.json` — a bare minimum file: name is `my-new-app`, version `0.0.1`, no dependencies, no scripts.

**What's missing:**
Everything. No source code, no dependencies, no framework, no `docs/ai/` directory or files. The AGENTS.md is a shell waiting to be filled in based on what you're actually building.

**First question:**

What is this project? Tell me what you're building — the problem it solves, who uses it, and (if you know) the tech stack you want to use. That'll let me fill in the AGENTS.md and create the `docs/ai/` files with useful, accurate content instead of generic placeholders.
