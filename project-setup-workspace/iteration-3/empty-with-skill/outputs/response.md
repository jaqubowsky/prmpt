Here's what I found in the project:

- The project is essentially empty — only two files exist: `AGENTS.md` and `package.json`
- `AGENTS.md` is a blank template with placeholder comments throughout (all sections say "Generated by Claude Code during setup")
- `package.json` has a name (`my-new-app`), version `0.0.1`, and `private: true` — no dependencies, no scripts, no stack defined yet
- No source files, no tests, no README, no `.git/` directory
- No framework, language, or toolchain has been chosen yet — this is a true greenfield project

I'll ask questions one at a time, starting with the product. I have a recommendation for each where I can.

---

**Question 1 of Phase A — What does this project do?**

Tell me what `my-new-app` actually is. Explain it like you'd tell a friend — what does it do, what problem does it solve, and who uses it?

(The name doesn't tell me much yet — there's no README, no source code, and no dependencies. I have no basis to guess, so I need to hear it from you.)
