Here's what I found in the project:

- **Essentially empty** — only two files exist: `package.json` and `AGENTS.md`, both are stubs with no real content.
- **`package.json`** declares a project named `my-new-app` at version `0.0.1` (private), but has no dependencies, scripts, or other configuration.
- **`AGENTS.md`** is a template placeholder — every section is a `<!-- Generated by Claude Code during setup -->` comment. Nothing has been filled in.
- **No source code, no tests, no docs, no git history, no framework** — this is a completely greenfield project with a name and nothing else.
- **No `docs/ai/` folder** exists yet — all six reference files (PRODUCT, ROADMAP, CONVENTIONS, PATTERNS, TESTING, ARCHITECTURE) need to be created from scratch.

I'll ask you one question at a time to build the full picture before writing anything. Let's start with the most fundamental:

---

**What does `my-new-app` actually do?**

Explain it like you'd describe it to a friend over coffee — not what tech it uses, but what problem it solves and who it's for. The name tells me nothing yet.

My working assumption until you tell me otherwise: this is a JavaScript/Node.js web app of some kind (given the `package.json`), but I have zero signal on the domain, the user, or the purpose.
