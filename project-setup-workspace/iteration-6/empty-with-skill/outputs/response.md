Here's what I found in the project:

- `package.json` exists with name `my-new-app`, version `0.0.1`, no dependencies, no scripts — a blank slate.
- `AGENTS.md` is a template with all sections as `<!-- Generated by Claude Code during setup -->` placeholders — nothing filled in yet.
- No source files, no folders, no tests, no framework, no toolchain of any kind.
- No `.git/` directory — this is not yet a git repo.
- This is a fully greenfield project. Every decision is still open.

I'll ask one question at a time to build a complete picture before writing anything.

---

**What does this app do — explain it like you'd tell a friend over coffee?**

Not the tech stack, not the architecture — just: what problem does it solve, and who has that problem? I'm asking this first because every other decision (framework, data layer, testing strategy, folder structure) depends on the answer.

*(If you're still figuring it out, tell me the rough idea — I'll push back and help you sharpen it.)*
