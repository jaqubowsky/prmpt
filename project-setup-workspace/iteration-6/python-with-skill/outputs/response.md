Here's what I found in the codebase:

- **Stack:** FastAPI + SQLAlchemy 2.0 + Pydantic v2 + Alembic + PostgreSQL (UUID primary keys), Python 3.11+. Ruff for linting, pytest + httpx for tests.
- **Domain:** This is an invoice API — a single `Invoice` model with `client_name`, `amount`, `status` (defaulting to `"draft"`), and `created_at`. Two endpoints exist: list all invoices and create one.
- **Status field is a plain string** — no enum, no validation. The value `"draft"` is hardcoded as a default. There's no way to transition an invoice to another state yet (e.g., `"sent"`, `"paid"`).
- **Missing pieces:** No `src/db.py` or `src/main.py` visible in the source tree — `get_db` and `Base` are imported but the files aren't there yet. The test uses an `AsyncClient` fixture (`client`) that doesn't exist in the test file.
- **AGENTS.md is a blank template** — all sections are `<!-- Generated by Claude Code during setup -->` placeholders. No `docs/ai/` directory exists yet.

I'll ask questions one at a time to build a thorough understanding before writing anything.

**First question — the product:**

Walk me through what this invoice API is actually for. Is this a standalone billing service for a SaaS product, an internal tool for a freelancer or small business, a backend for a specific frontend app — or something else entirely? The answer shapes almost every technical decision ahead (status transitions, auth, multi-tenancy, who calls this API and how).
